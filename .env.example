# .env.example - Exemple de configuration pour docker-brscan4
# Copiez ce fichier en .env et adaptez les valeurs
# Puis lancez docker avec : docker run --env-file .env ...

# === Variables OBLIGATOIRES ===
SCANNER_MODEL=                  # ← OBLIGATOIRE : Nom EXACT du modèle Brother (ex. MFC-L2700DW, DCP-L2540DW, MFC-J5620DW, etc.)
                                # Consultez support.brother.com → votre modèle → Downloads → Linux → Scanner Driver
                                # Si brscan4 apparaît → compatible. Utilisez le nom tel qu'affiché (sensible à la casse, tirets, etc.)

SCANNER_IP_ADDRESS=192.168.1.200 # ← OBLIGATOIRE pour scanners réseau : adresse IP fixe ou DHCP-réservée de l'imprimante/scanner

# === Variables optionnelles mais recommandées ===
SCANNER_NAME=Brother-Scanner    # Nom logique que vous voulez voir dans scanimage -L (défaut possible : SCANNER)

TZ=Europe/Paris                 # Fuseau horaire du container (utile pour les timestamps des scans)

PUID=1000                       # UID de l'utilisateur hôte → pour les permissions sur /scans (évite chown)
PGID=1000                       # GID de l'utilisateur/groupe hôte

# === Variables avancées / rarement changées ===
# DEBUG=1                       # Active plus de logs dans start.sh ou entrypoint.sh (à implémenter si besoin)

# Exemples de modèles courants compatibles brscan4 (non exhaustif – vérifiez toujours sur le site Brother) :
# DCP-L2500D, DCP-L2520DW, DCP-L2540DW, DCP-L2550DW
# MFC-L2700DW, MFC-L2710DW, MFC-L2740DW, MFC-L2750DW
# MFC-J5620DW, MFC-J5720DW, MFC-J5910DW, MFC-J625DW
# DCP-7060D, DCP-7065DN, MFC-L8850CDW, MFC-L8900CDW, HL-L2360DW (certains)